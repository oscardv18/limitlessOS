# packages/aur.txt — paquetes fuera de los repos oficiales, instalados con
# `paru`. Formato idéntico a pacman.txt. install/stages/20-aur.sh instala
# `paru` primero si no existe (es el único punto del instalador que compila
# algo antes de tener un AUR helper).
#
# A diferencia de pacman.txt, un fallo aquí NO detiene el instalador: cada
# paquete AUR es una compilación independiente, y una que falla no debe
# tirar abajo las otras.
#
# ── auditoría de nombres (hecha antes del despliegue) ──────────────────────
# Cada entrada de este archivo se verificó contra la API real de AUR
# (aur.archlinux.org/rpc/v5) Y contra la de los repos oficiales, porque el
# error caro no es escribir mal un nombre — es dejar aquí algo que YA está
# en los repos oficiales y hacer que paru lo compile desde cero sin motivo.
#
# Se movieron a pacman.txt por estar ya en `extra`: quickshell, lazydocker,
# impala, bluetui, wiremix, systemctl-tui, xh, rmpc.
# Se eliminaron por NO EXISTIR con ese nombre: hyprexpo, hyprbars (ver abajo).

# ── shell (§3.2) — completan lo que fish traía de fábrica ──────────────────
zsh-fast-syntax-highlighting
fzf-tab

# ── agentes / sesiones (§3.2, la integración más original del proyecto) ────
herdr

# ── datos y HTTP (§3.3) ──────────────────────────────────────────────────────
posting
lazysql

# ── theming ──────────────────────────────────────────────────────────────────
# matugen-bin declara conflicts/provides = matugen: no instalar ambos.
matugen-bin

# ── plugins de Hyprland — NO son paquetes ───────────────────────────────────
# `hyprexpo` y `hyprbars` estaban aquí y NINGUNO de los dos existe con ese
# nombre, ni en AUR ni en los repos oficiales. Habrían hecho fallar la
# etapa 20 en cada despliegue.
#
#   · hyprexpo  — es un plugin oficial del repo hyprland-plugins y se
#     instala con `hyprpm add https://github.com/hyprwm/hyprland-plugins`
#     + `hyprpm enable hyprexpo`. No hay paquete que instalar. Esto ya
#     estaba dicho en stow/hypr/.config/hypr/lua/plugins.lua, pero la
#     entrada equivocada seguía aquí.
#   · hyprbars  — existe en AUR como `hyprland-plugin-hyprbars`, pero el
#     proyecto lo DESCARTÓ a propósito (plan.md: "el semáforo de macOS no
#     pertenece a este lenguaje visual"). No se reintroduce.
#
# Los plugins de hyprpm se recompilan contra CADA versión de Hyprland: si
# el compositor se actualiza y el plugin no, Hyprland arranca sin él. Por
# eso no van en una lista de paquetes — necesitan `hyprpm update` tras
# cada actualización, y eso es trabajo de `dotctl update` (Fase 5).
